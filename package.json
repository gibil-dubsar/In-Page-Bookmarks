{
  "name": "in-page-bookmarks-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-static.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "chrome-types": "^0.1.292",
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.4.5"
  }
}
